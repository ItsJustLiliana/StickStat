import {describe,expect,it} from "vitest";import {readFileSync} from "node:fs";import {parseHockeyStandenMatches,parseHockeyStandenStandings,parseHockeyStandenTeam} from "../providers/hockeystanden";
const html=readFileSync(new URL("./fixtures/hockeystanden.html",import.meta.url),"utf8").replace("\"eventStatus\":\"https://schema.org/EventScheduled\"","\"eventStatus\": \"https://schema.org/EventScheduled\"");
describe("HockeyStanden parser",()=>{it("leest teammetadata uit JSON-LD",()=>expect(parseHockeyStandenTeam(html)).toMatchObject({name:"Rapide H1",clubName:"MHC Rapide"}));it("leest wedstrijden defensief uit SportsEvent",()=>expect(parseHockeyStandenMatches(html)[0]).toMatchObject({externalId:"2095780",homeTeam:"Rapide H1",awayTeam:"Liberty H2-O",status:"scheduled",competition:"Poule A"}));it("leest een eindstand uit de beschrijving wanneer de bron geen scorevelden heeft",()=>{const event=html.replace("\"eventStatus\": \"https://schema.org/EventScheduled\"","\"eventStatus\": \"https://schema.org/EventCompleted\",\"description\": \"Eindstand Rapide H1 4–4 Liberty H2-O\"");expect(parseHockeyStandenMatches(event)[0]).toMatchObject({homeScore:4,awayScore:4,status:"finished"});});it("leest de stand",()=>expect(parseHockeyStandenStandings(html)[0]).toMatchObject({position:9,team:"Rapide H1",played:20,won:6,drawn:2,lost:12,goalsFor:46,goalsAgainst:76,goalDifference:-30,points:17}));});
