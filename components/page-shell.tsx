import {AppNav} from "./app-nav";import {PlatformAdminProvider} from "./access-context";import type {TeamRole} from "@/generated/prisma/client";
export function PageShell({user,children}:{user:{name:string;photoPath:string|null;platformRole:string;teamMemberships:{roles:TeamRole[]}[]};children:React.ReactNode}){const admin=user.platformRole==="admin";return <PlatformAdminProvider value={admin}><div className="app-shell"><AppNav name={user.name} photoPath={user.photoPath} admin={admin}/><main className="content">{children}</main></div></PlatformAdminProvider>}
