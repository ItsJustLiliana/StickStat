import {AppNav} from "./app-nav";import {hasAnyTeamRole,teamManagementRoles} from "@/lib/team-roles";import type {TeamRole} from "@/generated/prisma/client";
export function PageShell({user,children}:{user:{name:string;photoPath:string|null;platformRole:string;teamMemberships:{roles:TeamRole[]}[]};children:React.ReactNode}){const manager=user.platformRole==="admin"||user.teamMemberships.some(membership=>hasAnyTeamRole(membership.roles,teamManagementRoles));return <div className="app-shell"><AppNav name={user.name} photoPath={user.photoPath} admin={user.platformRole==="admin"} manager={manager}/><main className="content">{children}</main></div>}
