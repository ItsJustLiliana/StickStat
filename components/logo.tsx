/* eslint-disable @next/next/no-img-element */
export function ClubLogo({name,path,size="normal"}:{name:string;path?:string|null;size?:"normal"|"small"}){const initials=name.split(/\s+/).map(x=>x[0]).join("").slice(0,3).toUpperCase();return path?<img className="logo" src={path} alt={`${name} logo`} style={size==="small"?{width:40,height:40,borderRadius:12}:undefined}/>:<div className="logo" style={size==="small"?{width:40,height:40,borderRadius:12,fontSize:13}:undefined} aria-label={`${name} logo`}>{initials}</div>}
