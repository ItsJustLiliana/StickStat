import Image from "next/image";export function ClubLogo({name,path,size="normal"}:{name:string;path?:string|null;size?:"normal"|"small"}){const initials=name.split(/\s+/).map(x=>x[0]).join("").slice(0,3).toUpperCase(),px=size==="small"?40:64;return path?<Image unoptimized className="logo" width={px} height={px} src={path} alt={`${name} logo`}/>:<div className="logo" style={size==="small"?{width:40,height:40,borderRadius:12,fontSize:13}:undefined} aria-label={`${name} logo`}>{initials}</div>}
