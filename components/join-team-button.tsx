"use client";
import {useState} from "react";
export function JoinTeamButton({teamId}:{teamId:string}){const [busy,setBusy]=useState(false),[message,setMessage]=useState("");async function join(){setBusy(true);setMessage("");try{const response=await fetch("/api/team-join-requests",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({teamId})}),body=await response.json();setMessage(response.ok?"Je aanmelding is verstuurd naar de teambeheerder.":body.error?.message??"Aanmelden mislukt")}catch{setMessage("Aanmelden mislukt")}finally{setBusy(false)}}return <div className="join-team-cta"><button className="button" type="button" disabled={busy} onClick={()=>void join()}>{busy?"Aanmelden…":"Sluit je aan bij dit team"}</button>{message&&<p className="muted" role="status">{message}</p>}</div>}
