"use client";import Link from "next/link";import {useState} from "react";import {useRouter} from "next/navigation";
export function LoginForm(){const [error,setError]=useState(""),[loading,setLoading]=useState(false);const router=useRouter();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const form=new FormData(e.currentTarget);const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:form.get("email"),password:form.get("password")})});const body=await r.json();setLoading(false);if(!r.ok)return setError(body.error?.message??"Inloggen mislukt");router.replace("/dashboard");router.refresh()}return <form className="login-form" onSubmit={submit}><span className="eyebrow">Welkom terug</span><h2 style={{fontSize:36,margin:"9px 0 5px"}}>Log in bij StickStat</h2><p className="muted">Bekijk de wedstrijd, ken de cijfers.</p><label htmlFor="email">E-mailadres</label><input className="input" id="email" name="email" type="email" autoComplete="email" required/><label htmlFor="password">Wachtwoord</label><input className="input" id="password" name="password" type="password" autoComplete="current-password" minLength={8} required/>{error&&<div className="error" role="alert">{error}</div>}<button className="button" disabled={loading}>{loading?"Bezig…":"Inloggen"}</button><p className="muted" style={{textAlign:"center",marginTop:18}}>Nog geen account? <Link className="link" href="/register">Registreer je</Link></p></form>}
