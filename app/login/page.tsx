import {currentUser} from "@/lib/auth";import {redirect} from "next/navigation";import {LoginForm} from "@/components/login-form";
export const dynamic="force-dynamic";export default async function Login(){if(await currentUser())redirect("/dashboard");return <main className="login-page"><section className="login-brand"><div className="brand"><span className="brand-mark">S</span>StickStat</div><div><span className="eyebrow" style={{color:"#c9f45b"}}>Your team. Your stats.</span><h1>Meer dan alleen de eindstand.</h1><p>Wedstrijden, spelers en prestaties. Eén helder teambeeld.</p></div><small>Gebouwd voor het hockeyveld.</small></section><section className="login-panel"><LoginForm/></section></main>}
