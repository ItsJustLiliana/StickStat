import {notFound} from "next/navigation";import {AttendanceList} from "@/components/attendance-list";import {PageShell} from "@/components/page-shell";import {pageContext} from "@/lib/page-data";import {db} from "@/lib/db";import {hasAnyTeamRole,teamManagementRoles} from "@/lib/team-roles";
export const dynamic="force-dynamic";
export default async function TrainingDetail({params,searchParams}:{params:Promise<{trainingId:string}>;searchParams:Promise<{team?:string}>}){const [{trainingId},query]=await Promise.all([params,searchParams]),{user,teams}=await pageContext(query.team),training=await db.training.findUnique({where:{id:trainingId},include:{team:true,attendance:true}});if(!training||!teams.some(team=>team.id===training.teamId))notFound();const membership=user.teamMemberships.find(item=>item.teamId===training.teamId),canManage=user.platformRole==="admin"||Boolean(membership&&hasAnyTeamRole(membership.roles,teamManagementRoles)),players=await db.player.findMany({where:{teamId:training.teamId,active:true,trainingMember:true},orderBy:[{lastName:"asc"},{namePrefix:"asc"},{firstName:"asc"}]}),status=new Map(training.attendance.map(item=>[item.playerId,item.status]));return <PageShell user={user}><div className="page-head"><div><span className="eyebrow">Training</span><h1>{training.title}</h1><p className="muted">{training.date.toLocaleDateString("nl-NL",{weekday:"long",day:"numeric",month:"long",year:"numeric"})}{training.startTime&&` · ${training.startTime}`}{training.endTime&&`–${training.endTime}`} · {training.venue??"Locatie onbekend"}</p></div></div>{training.notes&&<section className="card"><p style={{margin:0}}>{training.notes}</p></section>}<AttendanceList endpoint={`/api/trainings/${training.id}/attendance`} canManage={canManage} people={players.map(player=>({playerId:player.id,name:player.displayName,status:status.get(player.id)??"unknown",editable:canManage||player.userId===user.id}))}/></PageShell>}
