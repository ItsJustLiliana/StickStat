import {NextResponse} from "next/server";import {db} from "@/lib/db";export async function GET(request:Request){const release=await db.appRelease.findFirst({orderBy:{buildNumber:"desc"},select:{version:true,buildNumber:true,apkPath:true,sha256:true,notes:true,createdAt:true}});if(!release)return NextResponse.json({data:null});const downloadUrl=/^https?:\/\//i.test(release.apkPath)?release.apkPath:new URL(release.apkPath,request.url).toString();return NextResponse.json({data:{...release,downloadUrl}})}
