import {redirect} from "next/navigation";import {PageShell} from "@/components/page-shell";import {SyncButton} from "@/components/sync-button";import {currentUser} from "@/lib/auth";import {db} from "@/lib/db";
export const dynamic="force-dynamic";export default async function Admin(){const user=await currentUser();if(!user)redirect("/login");if(user.platformRole!=="admin")redirect("/dashboard");const [clubs,teams,users,runs]=await Promise.all([db.club.findMany({include:{teams:true}}),db.team.findMany({include:{club:true}}),db.user.findMany({include:{clubMemberships:true,teamMemberships:true}}),db.syncRun.findMany({include:{team:true},orderBy:{startedAt:"desc"},take:10})]);return <PageShell user={user}><div className="page-head"><div><span className="eyebrow">Platformbeheer</span><h1>Beheer</h1></div><span className="badge accent">PLATFORM ADMIN</span></div><div className="admin-grid"><section className="card"><div className="card-head"><h2>Clubs & teams</h2><span className="badge">{clubs.length} clubs</span></div>{clubs.map(c=><div className="sync-item" key={c.id}><div><strong>{c.name}</strong><div className="muted" style={{fontSize:12}}>{c.teams.map(t=>t.name).join(", ")||"Nog geen teams"}</div></div></div>)}</section><section className="card"><div className="card-head"><h2>Gebruikers</h2><span className="badge">{users.length} accounts</span></div>{users.map(u=><div className="sync-item" key={u.id}><div><strong>{u.name}</strong><div className="muted" style={{fontSize:12}}>{u.email} · {u.platformRole} · {u.teamMemberships.length} teams</div></div></div>)}</section></div><section className="card" style={{marginTop:18}}><div className="card-head"><h2>Synchronisatie</h2><span className="badge">IEDER UUR</span></div>{teams.filter(t=>t.externalProvider).map(t=>{const last=runs.find(r=>r.teamId===t.id);return <div className="sync-item" key={t.id}><div><strong>{t.club.name} · {t.name}</strong><div className="muted" style={{fontSize:12}}>{last?`${last.status} · ${last.startedAt.toLocaleString("nl-NL")} · ${last.newMatches} nieuw / ${last.updatedMatches} bijgewerkt`:"Nog niet gesynchroniseerd"}{last?.error&&` · ${last.error}`}</div></div><SyncButton teamId={t.id}/></div>})}</section></PageShell>}
